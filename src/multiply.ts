/*
This function multiplies two numbers together
parameters:
    - x (number)
    - y (number)

returns:
    - a value of type number
 */
let multiply = (x:number, y: number): number => x * y;

 // testing function with outputting values
console.log(`3 x 4 = ${multiply(3,4)}`)
console.log(`23.5 x 89.5 = ${multiply(23.5,89.5)}`)
